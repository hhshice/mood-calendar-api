[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["uv sync --frozen --no-dev"]

[phases.build]
cmds = ["uv run alembic upgrade head || true"]

[start]
cmd = "uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
